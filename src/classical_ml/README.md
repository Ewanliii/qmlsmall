# Classical ML Baselines

This folder contains classical baseline models used for comparison.

## Script

- `ml_work.py`  
  Runs Logistic Regression (LR), Random Forest (RF), and MLP on the same small datasets used by QML.
  Evaluation is performed with repeated stratified cross-validation and AUC reporting.

## Inputs

Either:
- a fixed small dataset `.npz` (recommended), or
- full latent arrays `Z` and labels `y` (local only).



```python

```
